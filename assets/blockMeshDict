// vim: ft=foam
// Automatically generated by Tetris v0
FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      blockMeshDict;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

convertToMeters {{ scale }};

vertices
(
    {%- for v in vertices %}
    {{ v.write() }}{% endfor %}
);

blocks
(
    {%- for b in blocks %}
    {{ b.write() }}{% endfor %}
);

edges
(
    {%- for e in edges %}
    {{ e.write() }}{% endfor %}
);

boundary
(
);

patches
(
);

mergePatchPairs
(
);

// ************************************************************************* //

